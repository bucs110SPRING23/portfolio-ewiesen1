import pygame
pygame.init()
screen = pygame.display.set_mode()
screen.fill("red")
pygame.display.flip() #keeps track of display image and next image
input("Press enter to continue...")
screen.fill("green")
pygame.display.flip()
input("Press enter to continue...")
#keeps track of display image and the next image